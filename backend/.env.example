# Django settings
# Generate a secure secret key using: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here-MUST-CHANGE-IN-PRODUCTION
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database settings (SQLite is default, no additional config needed)
# DATABASE_URL=sqlite:///db.sqlite3

# CORS settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# JWT settings
# JWT_SECRET_KEY=your-jwt-secret-key-here
